server_host = localhost
search_base = ou=users, dc=syncloud, dc=org
query_filter = (uid=%u)
result_attribute = uid
result_format = %u
version = 3
bind_dn = dc=syncloud, dc=org
bind_pw = syncloud
debuglevel = 2
